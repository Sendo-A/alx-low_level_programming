#include "main.h"
/**
 * _islower - function that checks for lower case characters
 *
 * @l: ro check the input
 *
 * Return: either 1 is a lower 0 not
 */

int _islower(int l)
{
	if (l >= 97 && c <= 122)
		return (1);
	return (0);
}
