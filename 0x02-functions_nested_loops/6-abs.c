# include "main.h"
/**
 * _abs - function that computes the absolute value of an intg
 *
 * @j: as an input for the function
 *
 * Return: an int
 */
int _abs(int j)
{
	if (j < 0)
		j = (-1) * j;
	return (j);
}
